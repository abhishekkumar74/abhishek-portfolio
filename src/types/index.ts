// ============================================================
// TYPES — Shared TypeScript interfaces used across the project
// These are re-exported from data files for convenience
// ============================================================

// Re-export from data files so components can import from here
export type { Project }      from '../data/projects';
export type { ExperienceItem } from '../data/experience';
export type { Certificate }  from '../data/certificates';
